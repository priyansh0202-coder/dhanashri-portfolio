
import React from "react";
export const metadata = {
    title: "Dhanashri Portfolio",
    description: "Infusyx Case Study",
};

export default function PayloLayout({ children }) {
    return <>{children}</>;
}