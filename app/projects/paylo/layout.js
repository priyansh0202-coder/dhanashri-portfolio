import React from "react";
export const metadata = {
    title: "Dhanashri Portfolio",
    description: "Paylo Case Study",
};

export default function PayloLayout({ children }) {
    return <>{children}</>;
}